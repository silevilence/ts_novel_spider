# 项目开发路线图 (Roadmap)

## 📅 计划中

- [ ] **浏览器插件采集桥接（用户授权访问）**：浏览器扩展作为本地服务端的受控采集传输端，适配受 Cloudflare 等访问验证保护、服务端无法稳定直连的网站
    - [ ] 扩展仅承担传输层：在真实浏览器顶级标签页渲染目标页面（用户可完成站点验证），序列化渲染后 DOM 回传；站点解析全部复用服务端现有 `SpiderAdapter`（经 `SpiderHtmlFetcher` 接缝注入浏览器传输实现），现有 syosetu/syosetu18/kakuyomu 适配器零改动即可经浏览器传输运行
    - [ ] 服务端默认仅监听 `127.0.0.1`（docker-compose 显式 `HOST=0.0.0.0` 不受影响）；扩展设置页填写本机后端地址与一次性配对令牌（短 TTL），配对后换取长期密钥，仅授权独立的 `/api/browser/*` 采集通道，可随时解绑撤销
    - [ ] 扩展与服务端经 WebSocket 双向通道通信（任务派发、页面 HTML 传输、状态回传同通道）；走查控制（打开目录/保存当前章节/暂停/继续/中止）在扩展弹窗，预览展示、章节多选、任务下发与大盘监控在 web 采集工作台
    - [ ] 浏览器采集任务复用现有任务快照/日志/SSE 大盘体系（kind=browser），同一时刻仅执行一个浏览器采集任务，后续排队；扩展断连即任务失败（transport_disconnected），保留进度可重试，重试跳过已成功章节，幂等入库复用现有内容哈希/版本历史机制
    - [ ] 章节走查为半自动模式：扩展依次打开未保存章节并自动保存，用户可随时暂停；验证页/限流由扩展轻量探测并回传类型化信号，任务进入「等待人工处理」暂停态，用户完成验证后继续并重试该章；页面解析异常自动停止并提示人工处理
    - [ ] 浏览器采集书籍不参与定时更新（增量更新经扩展手动走查 + 目录差分触发）；v1 不缓存图片媒体，正文保留远端 URL 由浏览器回源渲染；新增持久化采集审计表（任务范围、源站、时间、状态、失败原因）并提供查询
    - [ ] 站点接入扩展点 = 服务端注册可声明「浏览器传输」的适配器（新站点仍按现有 SpiderAdapter 规范开发，作为独立后续任务）
    - [ ] 扩展开发数据导出工具（供适配器开发与 fixture 采集）
        - 任务描述：内置用户手动触发的开发者导出——导出当前页渲染后 HTML 至本地文件，并可查看/复制页面 Cookie；数据仅落开发者本机、绝不回传服务端（见 ADR-0004，对 ADR-0002「凭据不出浏览器」的明确例外）；为 SyosetuOrg 等新站点适配器开发提供 fixture 采集与 Cookie 需求调研（SyosetuOrg 任务依赖此工具）

- [ ] **精翻任务原文同步**：当源小说有新增章节或内容修订时，提供「同步原文」功能将最新原文增量合并进精翻任务（保留已译段落映射，新增章节标记为待译），避免重建任务

- [ ] **SyosetuOrg 数据源适配与爬虫研发（与浏览器桥接联合交付）**：为独立小说浏览站 syosetu.org 新增采集能力；该站位于 Cloudflare 之后、服务端直连被 IP 封锁（403），适配器仅做解析，生产取数经浏览器桥接或放行代理（见 ADR-0003）
    - [ ] 调研与逆向分析
        - 任务描述：基于三本示例书（353455 / 391203 / 286002）调研信息页/目录页/章节页 DOM 结构、目录分页与部/卷层级、年龄确认门（Cookie 要求）、章节 URL 规律与正文段落结构；确认直连 403 下可行的取数传输（代理或桥接）
    - [ ] 测试 fixture 采集（经扩展开发数据导出）
        - 任务描述：在扩展「开发数据导出」工具（见 ADR-0004）就绪后，用其对三本示例书导出信息页、目录页与代表性章节页 HTML（含 Cookie 状态）作为 fixture；测试沿用 `createFixtureFetch` 内联 HTML 注入惯例，不发起真实网络请求
    - [ ] 核心适配器编码实现
        - 任务描述：继承 `BaseHtmlSpiderAdapter`，`sourceId = 'syosetu-org'`，经 `SpiderHtmlFetcher` 接缝取数、不含任何 Cloudflare 绕过；实现元数据、目录索引（含分页遍历与卷标题传播）、章节正文解析；在 `createDefaultSpiderRegistry()` 注册（label「syosetu.org」，默认示例号 353455）；声明 browser-transport 能力（注册表能力标记由桥接任务新增）
    - [ ] 全链路功能验收与回归测试
        - 任务描述：与浏览器桥接任务联合验收——桥接以 syosetu.org 三本示例书端到端采集为验收载体；解析测试覆盖信息页/目录页/章节正文结构与异常边界（目录分页循环、空目录、单章失败隔离）
    - 验收：`npm run test:server` 中 syosetu-org 用例全绿且 `npm run typecheck` / `npm run build` 通过；采集工作台可见「syosetu.org」来源，预览/下发正常；经浏览器桥接端到端抓取至少一本示例书完整入库（元数据+目录+正文），重复抓取可检出新增章节

## 🚧 开发中

## ✅ 已完成

- [x] **项目初始化与基础架构**
    - [x] 初始化 Node.js 根项目
        - 构建 `package.json` 并配置工程化运行脚本
        - 配置 TypeScript 开发环境及严格编译规范
        - 引入 `concurrently` 等依赖以支持多进程并行开发
    - [x] 搭建 Express 基础服务端
        - 于 `src/server` 目录搭建基础 HTTP 监听服务
        - 配置静态资源中间件，代理并托管前端构建产物
    - [x] 初始化 React 前端工程
        - 于 `src/web` 目录基于 Vite 构建 React 单页应用
        - 映射 Vite 构建输出目录至服务端静态资源伺服路径

- [x] **日志适配器策略模式实现**
    - [x] 制定日志采集接口规范
        - 设计兼容爬虫运行周期的全链路事件上报机制
        - 借鉴旧版 Python 架构（`C:\Users\silev\Documents\GitHub\PyNovelSpider`）的日志模型，并结合现存业务进行解耦重构
    - [x] 开发 Mock/测试日志适配器验证链路

- [x] **爬虫核心适配器与策略模式抽象**
    - [x] 选型并引入标准的 HTML 文档解析库
    - [x] 构建 SQLite 本地数据库及对象映射（数据表结构设计）
    - [x] 定义 Spider 核心策略接口契约
        - [x] 封装目标信息页 URL 路由生成逻辑（基于 `String` 类型 ID）
        - [x] 抓取并解析元数据（小说基础信息）
        - [x] 抓取并解析目录索引（支持多层级章节结构提取）
        - [x] 实现章节内容的精准抓取（单章模式）
        - [x] 实现章节内容的并发与聚合抓取（批量模式）
        - [x] 封装持久化逻辑（同步入库至本地 SQLite）
    - [x] 开发 Mock/测试爬虫适配器以验证核心调度逻辑

- [x] **Syosetu & Syosetu18 业务爬虫实例开发**
    - [x] 继承并实现 Spider 核心接口
        - 迁移并重构原 `C:\Users\silev\Documents\GitHub\PyNovelSpider` 库中的 DOM 解析逻辑
        - 依托原业务侧 `syosetu_spider.py` 与 `syosetu_18_spider.py` 代码，实现 TypeScript 版本的具体策略类
    - [x] 补充站点解析测试，覆盖信息页、目录页与章节正文结构

- [x] **前端交互界面与管控中心开发**
    - [x] 视图层基础功能与数据展板
        - [x] 渲染爬虫策略选择器（支持动态挂载多类爬虫实例）
        - [x] 构建任务输入控件（接收目标 ID 参数）
        - [x] 实现元数据看板，呈现解析后的核心信息（书名、作者、摘要、标签、章节总数等）
        - [x] 渲染多级目录结构视图（支持卷/大章的分级懒加载或预先渲染）
            - **增量比对逻辑**：优先检出本地 SQLite 缓存快照；结合远端新抓取的元数据，进行 Diff 校验，并高亮标记状态变更与新增章节
    - [x] 采集任务调度与生命周期管控
        - [x] 构建章节粒度的多选控件与下发触发器
        - [x] 实现异常隔离机制（确保单节点章节失败不引发全局任务阻塞）
        - [x] 提供任务容错交互（支持失败节点的单点重试或全量重新下发）
        - [x] 实现后台守护态运行逻辑（前端视图销毁不干扰底层后端采集进程）
    - [x] 实时监控与日志呈现矩阵
        - [x] 封装前端日志桥接适配器，订阅并解析服务端回传的任务流状态
        - [x] 基于日志通道构建高可用、低延迟的进度条及运行监控大盘

- [x] **网络代理策略接入与配置化**
    - [x] 核心网络采集层适配代理路由调度（保障抓取请求经由指定代理链路安全出站）
    - [x] 前端管控中心增设网络代理配置模块（支持代理参数的录入、状态校验与全局下发）

- [x] **前端路由重组与 UI/UX 体验升级**
    - [x] 全局视觉与表现层重构
        - [x] 实施全局暗黑模式（Dark Mode）视觉规范重写
        - [x] 剥离并清理页面残留的开发期占位元素与无效 Mock 数据
    - [x] 路由架构调整与模块解耦
        - [x] 将原有聚合页面横向拆分为「核心操作台」、「任务监控大盘」与「系统偏好设置」三个独立路由页面
    - [x] 核心交互链路（人机工程）改造
        - [x] 重构表单与操作按钮布局（建议引入吸顶/吸底或悬浮组件，消除长列表跨屏滚动导致的操作割裂感）
        - [x] 建立标准化的全局状态反馈机制（任务下发后提供明确的 Toast 文本提示或执行动效，消除运行状态盲区）
    - [x] 操作台视窗空间与排版密度优化
        - [x] 构建高密度目录树视图（引入按卷/大章层级的收缩与展开控制交互）
        - [x] 增设非核心面板（如未激活的爬虫模型配置区）的折叠态机制，有效降低视窗内的视觉噪音

- [x] **本地书库与离线资产管理体系**
    - [x] 架构规范与独立路由建设
        - 构建独立书库路由页面（作为已沉淀资产的全盘管理枢纽）
        - 强制执行前端组件最大化复用策略（严格复用原「核心操作台」的目录树组件与通用业务逻辑）
    - [x] 核心资产信息展现链路
        - 实现书库大盘列表视图（透出书名、数据源、作者、摘要、核心标签等业务元数据）
        - 构建单本资产详情页（聚合基础元数据与采集快照；全量映射多级目录树，并高亮区分已落地与待抓取的数据节点状态）
    - [x] 离线阅读引擎与多媒体流转控制
        - 研发已采集章节的沉浸式离线阅读器视图
        - 制定图片等富媒体资源的动态解析与本地化调度策略（支持主动触发物理落盘并映射本地加载路径；未缓存资产自动降级回落至源站外链渲染）
        - 建立多媒体状态标识追踪机制（在目录列表与阅读视图中，透出富媒体包含标识及其本地化状态，并支持定向触发媒体的下载任务）
    - [x] 资产增量同步与任务调度
        - 详情页集成操作枢纽，支持直接跨组件唤起元数据的增量更新与缺失章节的自动补录抓取

- [x] **数据序列化与本地导出引擎**
    - [x] 于单本资产详情页挂载多协议文件导出服务
    - [x] 目标产物构建规范与格式适配策略
        - [x] **Markdown 结构化文档构建**：映射标准语义树（H1 承载书名与前置元数据，H2 承载实际卷名或兜底的虚拟总卷，H3 承载章节标题）；本地化图片抽离至附属的 `./assets` 目录中输出，未缓存资源则降级保留源站绝对链接。
        - [x] **EPUB 电子书打包规范**：复用结构化语义进行标准转化，并执行本地多媒体资产的全量内联打包封装（仅保留源站链接的资源除外）。
        - [x] **TXT 纯文本扁平化输出策略**
            - 展平多级目录结构：强制抹除（忽略）原有的卷/分部映射，采用绝对递增的全局顺延章节序号（示例：跨卷时，由第99章直接映射生成第100章标题）。
            - 实施标准化排版格式：保证每章标题单行独立；正文内容按自然段断行。
            - 确保文本纯净度：章节与段落间仅保留自然换行符，严格剔除冗余的分割线、水印、导出时间等非正文干扰因素。

- [x] **应用视觉标识（Favicon）设计与配置**
    - [x] 基于系统核心业务特征完成产品图标视觉设计（要求输出标准化的 SVG 矢量资产）
    - [x] 实施前端工程接入，将产出资产配置并挂载为 Web 系统的全局站点图标（Favicon）

- [x] **容器化部署与编排体系构建**
    - [x] 制定生产级镜像构建标准与核心策略
        - 实施多阶段构建（Multi-stage Build）隔离前后端编译，确保输出生产态最小化资源占用包
        - 植入容器健康检查（Health Check）探针以支持服务存活性与可用性监控
    - [x] 编写 `Dockerfile`：生产环境镜像构建标准脚本
    - [x] 编写 `docker-compose.yml`：生产环境容器编排推荐模板（固定消费 `ghcr.io/silevilence/ts_novel_spider` 远端镜像）
    - [x] 编写 `Dockerfile.dev`：本地开发与联调专属构建脚本（全链路注入国内基础软件源与包管理器依赖加速源）
    - [x] 编写 `docker-compose.dev.yml`：本地测试环境容器编排配置（定向代理至 `.dev` 脚本执行即时构建与拉起）

- [x] **CI/CD 持续交付流水线研发 (GitHub Actions)**
    - [x] 建立全局 Fail-fast 熔断机制（流水线内任意环节如：日志提取失败、健康探针未通过等异常，均强制中断并阻断后续发布链路）
    - [x] 配置版本化触发器，监听并拦截标准 Version Tag 推送事件（支持忽略大小写规则匹配，如 `v1.1.0`）
    - [x] 动态解析 `CHANGELOG.md`，自动化提取当前目标版本的迭代声明
    - [x] 执行生产级 Docker 镜像构建任务
    - [x] 运行冒烟测试与准入校验（基于 Health Check 探针严格验证生成镜像的正常可用性）
    - [x] 推送构建产物至 GitHub Packages 容器注册中心 (`ghcr.io`)
    - [x] 自动创建并发布 GitHub Release 节点（同步挂载已提取的迭代声明内容）

- [x] **系统全局偏好设置拓展**
    - [x] 接入大模型(LLM)服务提供商(Provider)配置中心
        - 引入 `ai-sdk` 统一封装底层模型通信适配层
        - 支持单 Provider 挂载多模型实例（One-to-Many 映射架构）
        - 定义模型能力标识枚举（对话生成、向量嵌入/Embedding、重排序/Rerank），支持手动配置或基于 API 元数据自动嗅探归类
        - 提供各模型实例的连通性及鉴权状态探测接口（Ping/Test）
    - [x] 接入 Neo4j 图数据库连接配置
        - 提供数据库 URI 与凭证录入表单，并封装连通性健康探测接口

- [x] **本地书库功能矩阵扩容**
    - [x] 研发本地书籍的高级检索模块
        - 支持基于书名、数据源、标签(Tag)及摘要的多维度检索，其中书名、标签与摘要需支持模糊匹配
        - 引入相关性计分机制，搜索结果按匹配度(Relevance)降序排列
        - 集成高级查询语法解析器（支持布尔逻辑 `+`, `-`, 空格及特定字段过滤，如 `name:abc (tag:ddd or tag:eee) -site:syosetu`）
        - 提供可视化的查询语法引导说明（触发式气泡提示/ToolTip）
    - [x] 书籍别名(Alias)映射机制
        - 提供自定义书籍别名配置入口
        - 将别名纳入书名检索权重体系，实现别名与原名的等效匹配查询
    - [x] 离线阅读状态持久化与书签管理
        - 实现最高阅读进度“水位线”记忆（高水位持久化，回溯阅读不覆盖最新进度）
        - 挂载章节级书签的增删改查(CRUD)操作枢纽
        - 支持为书签附加自定义文本备注信息

- [x] **实体关系图谱(知识图谱)与 AI 伴读模块**
    - [x] 挂载基于单本资产的知识图谱生成引擎
        - **模型链路配置策略**：
            - 全局默认路由：于设置中心设定图谱生成所需的基础模型链路（对话、向量、重排序模型的默认网关）
            - 局部实例覆盖：支持为单本书籍配置专属的图谱生成模型链路
            - 配置防篡改锁定：图谱数据一旦实例化，锁定该书籍关联的模型配置以保证向量空间特征的一致性
        - **图谱引擎执行逻辑**：
            - 自动注入并调用全局配置的 Neo4j 数据库实例进行数据落盘
            - 建立命名空间隔离机制，确保不同书籍的图谱拓扑数据完全物理/逻辑隔离
            - 定义知识抽取规范：构建包含实体（人/事/物）及动态关系的图谱模型，且必须支持映射基于章节与故事内时间轴的拓扑演进状态（兼容非线性叙事结构提取）
        - **监控层UI**：
            - 详情页增设独立的图谱构建生命周期监控面板（展现实时生成进度与状态）
    - [x] 场景化 AI 伴读与智能检索助手(RAG)
        - 交互入口：设计全局悬浮式交互触点（右侧悬浮 FAB 按钮），唤起对话浮窗
        - 上下文感知：基于当前激活视图自动绑定业务上下文（信息详情页或阅读器页面）
        - Prompt 注入策略：
            - 详情页视图下，默认注入书籍全局元数据及摘要作为系统提示词
            - 阅读器视图下，支持动态追加当前章节内容切片（针对超长文本提供手动注入控制开关以防 Token 溢出）
        - 检索增强(RAG)与工具调用(Function Calling)：
            - 封装基于图谱拓扑关系的检索工具
            - 赋予大模型自主决策权，由 LLM 根据用户意图自行判断是否调用图谱工具及构建图查询语句

- [x] **阅读器排版与视觉偏好配置**
    - [x] 配置交互入口与实时渲染机制
        - 支持在全局设置中心进行统一调配，同时需在阅读器视图内挂载悬浮控制面板(Popover)支持快捷唤起，确保所有排版参数的变更均能即时响应并呈现所见即所得(WYSIWYG)的预览效果
    - [x] 字体族(Font-Family)与多语种适配机制
        - 支持自定义构建字体降级回退链路(Font Fallback Chain)
        - 内置涵盖中、英、日文复合语料的排版沙箱，用以实时校验多语种混合渲染效果
    - [x] 字号(Font-Size)动态缩放控制
        - 开放正文基础字号绝对数值(如 px/rem)的精准自定义映射
        - 预置标准化的 T-shirt 尺码（小/中/大档位）快捷切换面板，兼顾快速设置与高阶调整诉求
    - [x] 垂直排版间距参数调优
        - 行高(Line-Height)参数自定义配置
        - 段间距(Paragraph-Spacing/Margin-Bottom)参数自定义配置

- [x] **全局交互体验与视觉架构 (UI/UX) 重塑**
    - [x] 制定并执行全局页面重构与体验审查规范 (Design Guidelines)
        - 确立全局暗黑模式 (Dark Mode) 视觉基调
        - 构建清晰的视觉与操作层级 (Visual Hierarchy)：拉开主次操作按钮的视觉权重，核心业务内容高亮降噪，合理排布次要信息
        - 提升界面空间利用率：针对大区块的非核心信息引入折叠面板 (Collapse) 交互
        - 重塑交互链路与可用性 (Usability)：高频操作热区显性化，确保用户操作动线 (User Flow) 顺滑且符合用户心智模型
        - 规范产品文案体系 (UX Copywriting)：全面转换为用户视角表述，严格剔除研发底层黑话，同时保持垂直领域业务术语的专业度
    - [x] 全局导航体系交互升级
        - 实施顶部路由切换栏的吸顶悬浮 (Sticky Header) 改造，保障跨页面流转的全局可达性，同时严格约束组件纵向尺寸以最大化首屏可视区域
    - [x] 单本详情页浏览动线优化
        - 挂载常驻式全局悬浮控件 (FAB)，集成“置顶 (Back to Top)”、“直达底部”及“目录锚点跳转”功能，保障长页面浏览场景下的高效视图定位

- [x] **系统界面与用户体验 (UI/UX) 全链路重构**
    - [x] 启动全局 UI/UX 体验升级与视觉重构审查工程
    - [x] **全局架构与视觉基调设计**
        - [x] 引入 Mantine React 组件库重构前端页面，确立“暗色阅读主题 (Dark Reading Theme)”基调，融合“暖色纸质暗调 (Warm Paper Dark)”与“无干扰沉浸模式 (Distraction-Free Reading Mode)”的设计理念
        - [x] 重塑视觉层级 (Visual Hierarchy)，拉开主次元素的对比度，确保核心业务操作显性化透出
        - [x] 提升全局页面空间利用率与信息密度，优化组件间距，通过引入折叠面板 (Collapse) 等方式消除单面板纵向溢出（一屏无法全览）的布局缺陷
        - [x] 规范产品交互语料库，全面转化为用户视角文案，剔除底层研发黑话，同时保持专有名词的专业度
    - [x] **系统偏好设置中心重构**
        - [x] 菜单导航更名：废弃“下载设置”的局限性命名，升级为更具概括性的“全局设置”
        - [x] 交互体验调优：扩大折叠面板 (Accordion) 的触控热区，支持点击整行标题触发启停切换；压缩单面板纵向尺寸，防止超长列表撑爆视窗
        - [x] LLM 提供商与模型配置模块深度改造
            - [x] 采用“主列表+浮窗”的收纳式架构：外层视图仅透出 Provider 基础信息及挂载的 Model 概览列表；具体操作项（测试/设置/删除）收敛为图标组件并辅以 Tooltip 提示，极限压缩视觉空间
            - [x] 优化实例录入链路：手动添加采用独立 Modal 表单交互；API 远端拉取采用 Modal 列表展示，支持批量勾选导入后闭环关闭浮窗
            - [x] 废弃“默认承担能力”的手动分配逻辑，改为基于 API 元数据自动嗅探并渲染模型受支持的能力标签 (Capability Tags)
            - [x] 抽离全局模型路由网关：在 Provider 配置层外围增设独立的“默认模型网关”配置项，按模型能力标识（对话、嵌入、重排）提供精准的下拉筛选器组件
    - [x] **核心资产库模块 (Library) 交互升级**
        - [x] 书库列表视图 (List View)
            - [x] 规范化卡片布局尺寸：对简介、标签等动态内容区域实施定高与文本溢出截断 (Text-overflow: ellipsis) 处理，强制统一所有数据卡片的视觉高度
            - [x] 建立卡片内文本排版层级：通过字重与色彩对比度，显著区分标题、作者、标签与摘要等元数据
        - [x] 书籍详情视图 (Detail View)
            - [x] 执行详情页模块的重组与瘦身：聚合高内聚度的业务模块，严格管控单个功能面板的默认纵向伸展阈值
            - [x] 微功能面板聚合改造：将别名配置、书签管理、翻译状态、图片缓存等“单状态展示+单一动作按钮”的碎片化功能，统一收敛至“元数据与快捷操作”聚合卡片内，降低视觉噪音
            - [x] 页面空间管控策略：针对知识图谱（含实体关系、图谱浏览及单书配置）等超大尺寸且交互相对低频的重型业务面板，默认采取收起 (Collapsed) 状态
            - [x] 锚点导航 (Scrollspy) 引擎升级：强化右下角悬浮导航功能，支持直达所有一级面板；采用动态路由注册机制进行底层扩展设计，确保未来新增特性面板可自动挂载至导航目录
        - [x] 阅读器沉浸视图 (Reading View)
            - [x] 响应全局重构工程，严格对齐“暗色纸质沉浸模式”的设计规范与色彩令牌 (Design Tokens)
    - [x] **抓取工作台与异步任务大盘调优**
        - [x] 小说抓取工作台：优化底部悬浮操作台尺寸规范，严格限制最大高度；增加移动端响应式适配机制，避免软键盘唤起时引发严重的视图遮挡或挤压
        - [x] 异步任务调度大盘：引入自适应视窗高度与局部滚动机制 (Local Scroll)，修复右侧任务流无限延伸导致的全局布局失衡
        - [x] 任务队列 UI 规范化：提升任务参数与状态标识（如：已完成）的字号及色彩对比度，修复状态 Tag 文本溢出换行的 Bug，确保单行紧凑渲染

- [x] **AI 驱动的自动化翻译流水线**（目前只实现了基于chat的翻译，整个流程都还没有，继续全量功能检查）
    - [x] 垂直领域术语库（Glossary）构建与管理机制
        - 规范化术语元数据结构：需涵盖源语言、目标语言（支持留空待译）、实体归属类型（人物/地名/组织/道具等特定专有名词）及业务注释，并预留动态字段扩展能力。
        - 实施命名空间隔离：单本资产拥有完全独立的专属术语库。
        - 实施双向运维机制：支持模型在流水线中动态抽取并沉淀术语条目，同时提供可视化的人工介入运维（CRUD）后台。
    - [ ] ~~核心 Agentic 翻译引擎与多智能体协同工作流~~（先放弃，之后重新规划）
        - **架构与角色配置**：
            - 推荐引入 LangGraph 等具有状态管理的 Agentic 框架承载多角色流转。
            - Agent 角色动态配置矩阵：支持为核心节点角色（术语提取专家、翻译执行专家、质量校对专家、终审专家）设定全局默认的模型基座，并在发起单次任务前支持实例级的参数覆写。
        - **基于 Function Calling 的 I/O 工具链解耦**：
            - 借鉴代码生成 Agent 的文件操作范式，封装领域专属工具集（如 `read_origin_file`, `create_translation`, `edit_translation` 等），实现大模型过程性对话文本与最终结构化翻译物料的物理隔离与精准落盘。
        - **多 Agent 协同工作流 SOP 与质量控制循环（QC Loop）**：
            - 严格遵循基于章节自然顺序的串行调度逻辑。
            - 术语前置调度：先由提取 Agent 解析当前章节增量术语并融合本地词库；若探测到缺失译文的条目，强制阻断正文流程，优先调度术语翻译任务。
            - 知识注入与上下文增强 (RAG)：翻译及评审阶段均需将完整术语表注入 Prompt 头部（结合 Prompt Cache 机制优化命中率），并下发 RAG 检索工具供模型调阅相关业务语境以统一行文风格。
            - Actor-Critic 质量迭代机制：执行“初翻 -> 交叉校对 -> 终审评分”的标准产线逻辑。
            - 自动熔断与重试策略：设定质量验收阈值（Score >= 80 且无强阻断性质的修改意见）。未达标产物由审查 Agent 挂载纠偏建议并路由回传给翻译 Agent 执行多轮迭代，直至通过验收方可落盘固化。
        - **状态监控与结构化对齐**：
            - 任务大盘监控：在详情页全景透出单章及全书视角的翻译生命周期与进度状态。
            - 双语结构化绑定：强制执行段落级（Paragraph-level）的源文本与目标译文映射绑定，为下游的视图渲染与离线导出提供底层数据支撑。
    - [x] 前端阅读器视图（View）双语适配引擎
        - 交互式阅读面板：在视窗右侧挂载悬浮式（FAB）布局控制组件。
        - 渲染模式热切换：支持源语言视图、纯译文视图以及段落级交替双语对照（Bilingual Paragraph-by-Paragraph）三种排版模式的实时无缝切换。
    - [x] 离线资产序列化（Export）双语扩展
        - 导出引擎策略兼容：在文件构造链路中植入多语言配置器，支持按需指定输出的语言形态（纯源文 / 纯译文 / 段落级双语对照格式）。

- [x] **Kakuyomu 数据源适配与爬虫研发**
    - [x] 调研与逆向分析
        - 解析目标站点 `kakuyomu.jp` 的网页结构与接口逻辑
        - 提取并记录作品详情、章节目录及正文分页的高可用抓取策略与防反爬特征
        - 基于两个典型路径（`/works/822139839856110454` 与 `/works/822139838857602966`）完成深度案例剖析并输出开发技术方案
    - [x] 核心适配器编码实现
        - 开发专用爬虫适配器模块 `kakuyomu-spider-adapter`，封装抓取调度与数据清洗逻辑并接入现有爬虫调度框架
    - [x] 全链路功能验收与回归测试
        - 执行端到端抓取测试，验证元数据、目录结构及正文内容的准确性与完整性
        - 建立异常处理与边界测试用例，确保反爬识别、限流及网络异常的平稳降级

- [x] **自动化定时更新调度体系**
    - [x] 核心调度引擎与执行逻辑研发
        - 实现定时任务触发器：根据用户配置的调度策略，轮询拉取所有已标记为“需定时更新”的书籍资产
        - 执行目录差分比对：拉取远端最新目录索引并与本地快照比对
        - 增量抓取自动化：若探测到新增章节，自动触发下游下载管线完成内容同步
        - 全生命周期可观测性日志：在任务触发、各执行阶段流转及最终回收的全链路节点植入结构化日志输出，统一接入控制台实时监控视图
    - [x] 系统偏好设置中心 —— 定时更新配置面板
        - [x] 调度策略配置器（三选一互斥模式）
            - [x] 固定间隔轮询模式
                - 支持按小时粒度设定轮询间隔
                - 交互式反馈：输入变更后即时计算并展示下一次预计触发时间
            - [x] Cron 高级表达式模式
                - 前端集成 Cron 表达式合法性校验引擎
                - 提供可视化的 Cron 快速编排 UI（Quick Config）
                - 实时预览面板：展示 Cron 后续 N 次触发时间戳作为配置参考
            - [x] 简易周期定时模式
                - 支持按“每周几”（多选）+“固定时刻”的组合规则设定触发计划
        - [x] 定时更新资产范围管理
            - 入口收敛：设置页面上收敛为一个“管理定时更新书单”的操作按钮
            - 交互形态：点击唤起 Modal 浮窗，内嵌全量本地书籍基础信息列表
            - 勾选逻辑：浮窗唤起时自动回填并勾选当前已加入定时更新的书籍
            - 用户可增量调整勾选集合，并通过“保存”或“取消”关闭闭环
    - [x] 单本资产详情页 —— 定时更新快捷开关
        - 在详情页视图挂载独立 Toggle 控件，用于覆盖当前书籍是否参与全局自动化定时更新调度

- [x] **OPDS 书源服务构建与分发**
    - [x] **多版本协议适配与端点暴露**
        - [x] 集成 `r2-opds-js` 核心依赖库，构建标准化的 OPDS 服务引擎
        - [x] 暴露 `/opds/v1` 端点，提供符合 OPDS 1.2 规范（基于 Atom XML）的书源服务
        - [x] 暴露 `/opds/v2` 端点，提供符合 OPDS 2.0 规范（基于 JSON-LD）的书源服务
    - [x] **元数据管理与可见性控制**
        - [x] 扩展书籍实体属性，新增“OPDS 可见性控制”布尔字段（默认设置为 `false`，即默认不公开分发）
        - [x] 新增书籍状态统一管理看板，支持对全量书籍的 OPDS 公开状态进行批量配置与精细化管理
    - [x] **电子书制品（EPUB）生命周期与调度引擎**
        - [x] 建立双时间戳跟踪机制，记录并维护每本书籍的“元数据/内容更新时间”（Update Timestamp）与“EPUB 制品生成时间”（Compilation Timestamp）
        - [x] 实现轻量级后台调度引擎（Cron Job），定时扫描启用 OPDS 服务的书籍实体，执行差分检查：当“更新时间”晚于“制品生成时间”时，触发后台自动重构，生成最新版 EPUB 制品
    - [x] **多语种多版本制品动态交付**
        - [x] 针对已完成翻译的书籍，支持原生（Original）、译文（Translated）及双语对比（Bilingual）三种 EPUB 制品的动态组装与交付（未翻译书籍仅提供原生版本）
        - [x] 在 OPDS 1.2（`/opds/v1`）架构下，通过单一 Entry 下挂载多组具有特定关系属性（Relationship Link）的 `<link>` 标签进行多版本分发
        - [x] 在 OPDS 2.0（`/opds/v2`）架构下，利用标准的分组容器（Groups Catalog）特性，实现结构化的多版本制品分发与导航
    - [x] **后台策略配置与可观测性看板**
        - [x] 在全局系统设置中，新增 OPDS 引擎专属配置项，支持自定义“后台差分扫描周期/间隔”（Cron Interval）
        - [x] 构建独立的可观测性监控面板，实时呈现 OPDS 服务运行状态（包含：当前上架书籍统计、分发版本分布图谱、差分扫描及制品生成审计日志等）

- [x] **定时更新增加自动翻译联动**
    - [x] 数据层：扩展 `scheduled_novels` 表，新增 `auto_translate` 布尔列（默认 `0`），同步更新 `StoredScheduledNovelRow` 接口与 Repository CRUD 方法
    - [x] 调度层：`SchedulingService.#checkSingleNovel()` 增量下载完成后，若该书 `auto_translate = true`，自动调用 `TranslationService.startTranslation()` 触发翻译
        - 前置校验：翻译配置已就绪（模型网关已配置）且当前无运行中的翻译任务；校验不通过时记录警告日志并跳过，不阻塞定时更新主流程
        - 翻译范围：`startTranslation` 内置的断点续译机制会自动跳过已完成章节，仅翻译新增章节
    - [x] 前端交互：定时更新书单管理 Modal 中为每本书增加「自动翻译」开关（仅当该书已启用定时更新时可操作）；单书详情页定时更新面板同步展示该状态

- [x] **定时更新管理页面独立与功能增强**
    - [x] 路由与页面拆分
        - 新增 `AppRouteId = 'scheduling'`，路径 `/scheduling`，导航标签「定时更新」，对齐 OPDS Dashboard 的独立页面模式
        - 从「全局设置」页面移除定时更新配置面板，改为新页面承载
    - [x] 全局调度策略配置（迁移自 SystemPreferences）
        - 三种调度模式选择器（固定间隔 / Cron 表达式 / 每周定时）及对应参数表单，复用现有 `CronEditor` 组件
        - 全局启用/禁用开关
        - 调用现有 `GET/PUT /api/control/scheduling` 接口，无需新增后端路由
    - [x] 定时更新书单管理（迁移自设置页 Modal）
        - 在页面内以表格/列表形式展开启用定时更新的书籍，支持批量勾选与单本开关
        - 每本书展示上次检查时间、检查结果（已是最新 / 发现 N 个新章节 / 出错）、检查消息
        - 调用现有 `GET/PUT /api/library/scheduling/novels` 接口
    - [x] 运行记录看板
        - 拉取 `scheduled_check_runs` 表数据，以时间倒序展示历史运行记录
        - 每条记录展示：开始时间、完成时间、耗时、扫描书籍数、发现更新数、跳过数、错误数
        - 新增后端路由 `GET /api/control/scheduling/runs`（支持 `?limit=&offset=` 分页），复用 Repository 已有的 `getLatestCompletedCheckRun()` 查询模式扩展为列表查询
    - [x] 小说级「更新总结」功能
        - 数据层：扩展 `scheduled_novels` 表，新增 `auto_summarize` 布尔列（默认 `0`）和 `summarize_model_json` 文本列（存储 `{ providerId, modelId }`，可选），同步更新 `StoredScheduledNovelRow` 接口与 Repository CRUD
        - 模型选择：页面顶部或书单区域提供全局总结模型下拉选择器，从已配置的对话类（chat）模型中选取；单本书可覆盖全局选择
        - 调度层：`SchedulingService.#checkSingleNovel()` 增量下载完成后，若该书 `auto_summarize = true`，调用 LLM 对本次新抓取章节生成简要总结（章节标题 + 关键剧情摘要）
        - 总结结果持久化：扩展 `scheduled_check_runs` 表或新增 `scheduled_summaries` 表，记录 `{ runId, sourceId, novelId, chapterIds, summary, modelId, createdAt }`
        - 前端展示：运行记录详情中可展开查看各书的章节总结内容；书单列表中对有总结的书籍显示摘要图标标识

- [x] **知识图谱检索链路向 GraphRAG 渐进升级**
    - [x] 子图摘要与章节簇摘要层
        - 为实体关系子图与章节簇生成可增量重建的摘要资产，作为高层检索入口
        - 在单本资产命名空间内维护 `subgraph summary` 与 `chapter cluster summary` 索引，并与原始章节片段建立可回溯映射
        - 在 AI 伴读回答阶段支持“先召回摘要、再下钻原文证据”的双阶段上下文拼装链路
    - [x] 图谱路径扩展与多跳召回机制
        - 引入 Query -> 实体对齐(Entity Linking) -> 邻域扩展 -> 路径评分 的图检索流程
        - 支持围绕人物、地点、组织、概念等核心实体执行 1~2 跳关系扩展，并回溯至章节级证据片段
        - 融合关系权重、向量相似度、章节时间顺序及重排序结果，构建统一的多跳召回评分模型
    - [x] 全局 / 局部双模检索编排器
        - 局部问题优先走章节感知(Local)检索，聚焦当前章节、邻近章节与命中的局部子图
        - 全局问题优先走全书视角(Global)检索，基于章节簇或主题簇摘要汇总主线、势力与主题演进
        - 由编排器或 LLM 根据用户意图动态选择检索模式，并在前端透出可解释的检索轨迹与命中来源
    - [x] 社区级 GraphRAG 完整演进
        - 引入图社区划分、社区摘要与层级化汇总机制，构建更接近标准 GraphRAG 的全书知识组织结构
        - 支持图谱重建、摘要重算与 Neo4j 同步的增量调度，控制长篇作品的构建时延与资源消耗
        - 建立问答评测集与回归基线，重点验证全局总结、多角色关系推理及跨章节多跳问题的收益

- [x] **精翻功能（Refined Translation Workspace）**
    - [x] 独立任务化翻译工作区
        - 与现有粗翻（小说详情页翻译）完全解耦：独立路由页面、独立数据表、独立制品资源，互不影响书籍详情页的下载与翻译状态
        - 以「任务」为基本单位：每本小说可创建多个精翻任务，任务间数据隔离，各自维护术语表、译文、审核记录与导出制品
        - 任务自包含：原文快照、译文、术语表均存于任务专属表，不依赖源小说是否存在于书库（源小说删除后任务仍可查看与导出）
        - 任务持久化：所有进度、过程、结果按任务保存，支持随时回到任务进行查看、修改、导出
    - [x] 任务删除与回收站
        - 标记删除（假删）：任务移入回收站，页面上专门展示回收站任务列表；回收站内可查看与导出，不可修改
        - 可取消标记恢复任务；重新标记重新开始计时
        - 标记后 15 天可真删：级联清理任务所有关联数据（术语表、译文、审核记录、checkpoint、操作日志），二次确认后不可恢复
    - [x] 任务创建与基础配置
        - 选择一本已抓取的小说作为翻译源
        - 原文提取复用详情页翻译的原文切分逻辑（按段落切分），创建任务时将切分结果快照至任务中（段落划分与内容均独立），此后不再随源站变动或粗翻切分逻辑变更而改动
        - 输入任务名（默认为「{小说名} 精翻任务 {日期}」）
        - 选择各环节使用的模型（可后续更改）：
            - 术语提取模型（从 RAG 实体抽取候选术语）
            - 术语翻译模型（生成术语初翻）
            - 初翻模型（正文初译，支持多模型并发）
            - 遗漏判定模型（判断未翻译项是否可跳过）
            - 审核模型（校对与评分）
        - 源语言/目标语言配置（默认沿用翻译偏好）
    - [x] 翻译工作区与 Agent 工具链
        - 不通过对话返回翻译，而是以工作区模式操作：Agent 通过工具读写翻译物料，翻译结果落盘至任务专属存储
        - 设计任务级翻译文件结构（SQLite 表 + 可选文件缓存），考虑持久化与读写性能（按 task_id / chapter_id / paragraph_index 建索引）
        - 提供 Agent 工具集：
            - `read_original_chapter`：读取章节原文（按段落）
            - `read_current_translation`：读取当前译文
            - `read_untranslated_segments`：读取未翻译/遗漏段落
            - `write_translation_segment`：写入段落级译文
            - `read_glossary` / `update_glossary_term`：读取/更新任务术语表
            - `read_review_issues`：读取审核意见
            - 审核专用工具：
                - `read_chapter_translation`：读取单章完整译文（段落级，含原文对照）
                - `read_context_chapters`：读取前后章节译文作为上下文（跨章一致性检查）
                - `write_review_result`：写入审核评分与意见（维度分数、意见列表、是否强制修改、轮次）
                - `mark_segment_issue`：对具体段落标记问题（关联意见到段落）
        - 前端提供工作区视图：原文/译文对照、未翻译项高亮、术语表编辑、审核意见展示
    - [x] 前端查看与编辑界面
        - [x] 任务列表与详情
            - 独立路由页面展示所有精翻任务卡片（任务名、小说、状态、进度、更新时间）
            - 点击进入任务详情，顶部展示任务状态条与当前步骤
        - [x] 术语表视图
            - 表格展示：源术语、译文、实体类型、优先级、AI 建议、状态（待确认/已确认/已排除）
            - 支持行内编辑译文、批量确认/排除、按状态/类型筛选、搜索
            - 「向 Agent 提意见」入口：单次请求-响应模式，用户提修改要求 → Agent 重新生成建议 → 用户确认采纳；多轮交互通过反复提意见实现（不开对话窗口）
        - [x] 译文对照视图
            - 段落级原文/译文并排或交替对照，未翻译/遗漏段落高亮标识
            - 行内编辑译文，支持撤销未保存的修改回滚到上次保存版本（不保留完整版本历史链）
            - 按章节导航，支持跳转到指定章节/段落
            - 「向 Agent 提意见」入口：单次请求-响应模式，选中段落后提修改要求 → Agent 修改 → 用户确认采纳；多轮交互通过反复提意见实现（不开对话窗口）
        - [x] 审核意见视图
            - 展示审核评分（各维度）与意见列表（严重等级、段落定位、建议）
            - 意见可关联跳转到对应段落，支持标记为「已处理/忽略」
        - [x] 任务操作区
            - 启动/暂停/继续/重试失败段落/导出/删除 等操作按钮
            - 操作日志时间线（步骤流转、异常、用户干预记录）
        - [x] 回收站视图
            - 展示已标记删除的任务列表，含标记删除日期与距真删剩余天数
            - 支持查看与导出（不可修改），支持取消标记恢复
    - [x] 翻译步骤流水线（基于 LangGraph 状态图）
        - [x] 流水线编排与状态机
            - 使用 LangGraph `Annotation.Root()` 定义精翻任务状态图，节点对应各翻译步骤，边表达步骤流转与条件分支（审核不达标回退至初翻）
            - 每个节点的输入输出与中间态持久化至 SQLite（checkpoint），支持服务重启后从最近 checkpoint 恢复
        - [x] 中断与恢复
            - 服务异常退出（如 down 机）后重启，自动检测未完成任务并从最近 checkpoint 继续，不丢失已完成段落
            - 每个段落翻译原子提交，避免半成品状态
        - [x] 异常处理
            - 网络异常、LLM 调用失败等单点错误捕获并标记段落为 `failed`，不中断整体流水线
            - 提供失败段落重试入口（单段/批量），重试时复用原上下文
        - [x] 暂停与继续
            - 支持用户主动暂停任务（保存当前进度并停止调度），继续时从暂停点恢复
            - 暂停/继续状态持久化，服务重启后保持用户意图
        - [x] 实时状态展示
            - 任务级状态：当前步骤、整体进度百分比、已译/失败/待译段落数、当前轮次
            - 步骤级状态：术语表进度、初翻进度、遗漏检查结果、审核评分与意见
            - 通过 SSE 推送实时进度，前端工作区实时刷新
        - [x] LangGraph 状态图可视化
            - 渲染状态图节点（各翻译步骤）与边（流转/条件分支），高亮当前执行节点与已完成节点
            - 可折叠/展开切换，默认折叠避免占用界面，用户需要时展开查看执行进度
            - 节点点击可下钻查看该步骤的详细输入输出与中间态
        - [x] 步骤一：术语表建立
            - 从小说既有术语库（粗翻 `novel_translation_terms`）与知识图谱（RAG）名词性实体读取候选术语，复制进任务专属术语表（按 task_id 隔离），之后与粗翻术语库互不影响
            - 去重合并后，AI 给出筛选建议（如推荐保留/排除、推荐实体类型与优先级），但最终由用户人工确认
            - 形成任务术语表，译文可留空待译；术语表在任务中持久化，支持后续增删改查
            - 流水线自动暂停等待用户确认，用户确认后手动点「继续」进入下一步
        - [x] 步骤二：术语翻译
            - 由翻译 Agent 批量生成术语初翻，注入上下文与实体类型以提升一致性
            - 用户可向 Agent 提修改意见或手动编辑，术语表完成后保存至任务
            - 流水线自动暂停等待用户确认，用户确认后手动点「继续」进入下一步
        - [x] 步骤三：正文初翻（全自动）
            - 由翻译 Agent 按章节顺序进行段落级初翻，注入任务术语表（全量注入 prompt 头部以提升缓存命中）与上下文历史
            - 支持并发调度（按任务配置的并发数），单章失败不阻塞整体
        - [x] 步骤四：遗漏检查（全自动）
            - 代码检查：段落数量对齐、空译文、未翻译标记等结构性遗漏，产出遗漏项列表
            - Agent 判定：对每个遗漏项逐一判断是否可跳过（如专有名词、数字、URL 等直接使用原文）
            - 可跳过项：自动填入原文作为译文并标记 `skipped`
            - 需翻译项：回退到初翻步骤对该段落补译（注入术语表+上下文），补译后重新进入审核
        - [x] 步骤五：审核校对（全自动，按单章进行）
            - 审核粒度：单章审核为主，每章走完「初翻→检查→审核→（不达标）修改→重审」完整循环，单章通过即固化
            - 由审核 Agent 对译文进行校对与审核，术语表全量注入 prompt 头部，并可通过 `read_context_chapters` 读取前后章节做跨章一致性检查
            - 提出审核意见（含严重等级与段落定位，通过 `mark_segment_issue` 关联到段落）并给出最终评分
            - 评分维度：流畅度、一致性、术语准确度、格式规范度
        - [x] 步骤六：迭代收敛（全自动）
            - 轮次定义：完整循环（初翻+检查+审核）算第 1 轮；不达标后修改+重审算第 2 轮；以此类推
            - 当审核意见含强制修改项或评分 < 80 时，翻译 Agent 按意见修改后重新审核（进入下一轮）
            - 评分 >= 80 且无强制修改项时，该章该轮翻译通过
            - 设最大轮次上限（如 5 轮），超限后强制暂停并标记需人工介入，避免无限循环
    - [x] 手动微调与导出
        - 完成一轮翻译后，用户可手动修改译文、向 Agent 提要求修改、查看当前翻译结果
        - 导出时机：随时可导出，不要求任务完成；导出时取当前已通过审核（或至少已初翻）的章节
        - 导出：复用现有 Markdown / EPUB / TXT 导出引擎，但翻译内容取自精翻任务（独立于粗翻译文）
        - 导出支持原文/译文/双语对照三种模式；导出界面提供「仅已完成章节 / 全部章节（未完成用原文）」选项
        - 导出文件为临时生成供下载，不持久化制品（翻译内容本身已持久化在任务中，可随时重新导出）

- [x] **手动维护小说功能**
    - [x] **手动小说类型（Manual Novel）**
        - [x] 新增小说类型「手动小说」：元数据、目录、正文全部由用户手动维护，不依赖任何站点
            - [x] 标识：`source_id = 'manual'` + `novel_id = UUIDv4`，复用现有 novels/chapters 等全部数据表与路由
            - [x] 章节结构复用两级「卷 → 章」（对应现有 volume_title/chapters），支持「未分卷」章节
            - [x] 章节 `url` 与信息页 `info_page_url` 存空字符串 `''`（哨兵值，表结构不变）
        - [x] 手动小说不出现在自动抓取页面（无 SpiderAdapter，天然不可达）
        - [x] 手动小说不可加入定时更新书单（书单加载时过滤 `source_id = 'manual'`）
        - [x] 书库列表正常显示，卡片左上角 SOURCE 位显示 `manual` 即可（复用现有标识位）
        - [x] 书库页面提供「新建手动小说」入口：仅输入标题即可创建，其余元数据留空
        - [x] 详情页提供「编辑元数据」表单（标题/作者/简介/标签），区别于抓取小说的「同步元数据」
        - [x] 章节管理：在章节目录支持 卷/章 的新建、重命名、删除、卷内排序、跨卷移动（重算全局章节顺序）；删除卷时连坐删除其全部章节（强确认）
        - [x] 正文编辑：阅读页可修改章节内容，支持 Markdown 格式（GFM），编辑器用 `@uiw/react-md-editor`（编辑/预览分屏）
            - [x] 图片插入：先在本地预览（ObjectURL），保存时统一上传至 `data/manual-assets/{novelId}/`，正文以 `manual://{assetId}` 引用；任一图片上传失败则整体不保存（保留草稿）
            - [x] 保存规则：仅"保存成功且内容与当前版本不同"产生新版本；内容相同提示「内容无变化，未保存」；保存失败提示原因并保留草稿可重存；未保存离开需确认（确认后视为放弃修改）
            - [x] 图片文件在小说真删时随 `data/manual-assets/{novelId}/` 目录一并清理（第一版不做引用计数，孤儿文件容忍）
    - [x] **书库管理增强**
        - [x] **小说删除（书库回收站）**
            - [x] 删除即移入书库回收站（软删除，独立于精翻回收站）：可查看、可还原、不可修改
            - [x] 移入回收站即退出定时更新与 OPDS 分发（还原时按原状态恢复）
            - [x] 移入 15 天后可彻底删除：二次确认后真删，清理小说全部关联数据（章节、书签、进度、别名、图谱、翻译引用、OPDS 制品、offline-assets/manual-assets 对应目录、元数据与章节版本历史）；精翻任务因自包含快照而保留
        - [x] **元数据更新（仅抓取小说）**
            - [x] 详情页「同步元数据」：重新抓取远端元数据，与当前版完全一致则 toast「元数据无变化」；有差异则弹浮窗对比
            - [x] 浮窗逐字段选择：标题/作者/简介为「保留旧版 / 采用新版」二选一；标签为「保留旧 / 采用新 / 合并（去重并集 + 逐标签勾选，默认全选）」
            - [x] 未变化字段在浮窗中显示「未发生变化」；「确认应用」立即写库；「取消」丢弃本次同步
            - [x] 采纳的字段写入元数据变更历史，并给对应翻译单元打「待重译」标记
            - [x] 变更历史：每本书一条时间线（快照式、不可变、支持查看与还原）
            - [x] 重译联动：被标记「待重译」的单元在下一次「继续翻译」时纳入翻译目标（与新增章节增量翻译共用入口）；不自动触发、不全量重译；用户可手动触发全量重译
        - [x] **章节内容更新（仅抓取小说）**
            - [x] 章节目录每节右键菜单 + 阅读页入口，调用同一后端端点做单章重新抓取
            - [x] 远端为权威：内容变化则替换正文（标题一并更新），产生新版本，打「待重译」标记；无变化则提示「章节内容无变化」，不写库；抓取失败提示原因并保持现有内容
            - [x] 变更历史：每章一条时间线（快照式、支持查看与还原）；章节目录中发生过变更的章节显示「发生过 x 次变更」（v0 不显示）
        - [x] **版本历史通用机制（手动小说与抓取小说共用）**
            - [x] 版本号 = 变更次数，从 0 开始；初始版本（首次填写保存或首次抓取入库）为 v0，不显示变更数
            - [x] 每次「保存成功且内容不同」产生新版本（v1、v2…），版本不可变、快照式存储
            - [x] 手动小说无「同步元数据」「单章更新」功能，但有同样的版本历史：元数据首次填写保存 = v0，章节首次填写保存 = v0，此后每次保存递增
    - [x] **Markdown 全局升级（新功能兼容老功能）**
        - [x] 全库章节正文统一按 Markdown 处理（抓取小说正文为纯文本，是合法 MD 子集）
        - [x] 阅读器渲染升级：`react-markdown` + `remark-gfm` + `rehype-sanitize`（支持表格/删除线/任务列表，防 XSS）
        - [x] 导出升级：Markdown 原样透传；EPUB 用 `marked` 转 HTML；TXT 剥 HTML 标签保留纯文本
        - [x] 翻译段落切分升级：识别 GFM 表格块，整个表格作为单个段落单元切分（不拆行），其余按自然段
        - [x] 图片提取新增识别 `manual://{assetId}` 引用（渲染/导出/缓存管线复用现有逻辑）
        - [x] RAG 检索与图谱抽取保持文本层处理（不解析 MD AST），仅图片提取感知 `manual://`
